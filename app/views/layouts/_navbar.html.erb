<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#">SomeShop</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarText">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">About Us</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Support</a>
      </li>
    </ul>
    <span>
      <form class="form-inline">
        <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-light my-2 my-sm-0" type="submit">Search</button>
      </form>
    </span>
    <span class='user-nav-links'>
      <% if user_signed_in? %>
          <%= link_to 'Log Out', destroy_user_session_path, method: 'delete', class: 'ml-3 mr-3 text-light', id: 'log-out-link' %>
          <%= link_to "Cart (#{current_cart.item_count})", cart_path(current_cart), id: 'cart-link', data: { cartid: current_cart.id } if current_cart %>
        <% else %>
          <%= link_to 'Login', new_user_session_path, class: 'ml-3 text-light login-link' %>
          <%= link_to 'Sign Up', new_user_registration_path, class: 'ml-3 text-light sign-up-link' %>
      <% end %>
    </span>
  </div>
</nav>
